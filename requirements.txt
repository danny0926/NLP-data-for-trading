# Congressional Trading Intelligence System — 統一依賴清單
# 由 active Python 檔案的 import 掃描產出

# ── 核心 ──
python-dotenv==1.2.1
requests==2.32.5
pandas==2.3.3
pydantic==2.12.5

# ── LLM / AI ──
google-genai==1.63.0

# ── 資料抓取 (ETL) ──
curl-cffi==0.13.0
beautifulsoup4==4.14.3
playwright==1.58.0
PyMuPDF==1.27.1

# ── 分析 ──
yfinance==1.0
